import Papa from 'papaparse'; export default function UploadCSV({onParsed}){ async function handleFile(e){ const f=e.target.files[0]; if(!f) return; Papa.parse(f,{header:true,skipEmptyLines:true,complete:(res)=>onParsed(res.data)}); } return (<label className="block card"><div className="text-sm text-slate-600">Upload settlement CSV</div><input type="file" accept=".csv" onChange={handleFile} className="mt-2" /></label>);} 
