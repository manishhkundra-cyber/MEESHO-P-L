import {useState} from 'react'; export default function ColumnMapper({headers=[],onMap}){ const req=['SKU','Quantity','PriceInclTax','Commission','ShippingReceived','ReturnAmount']; const [map,setMap]=useState({}); return (<div className="card mt-4"><h3 className="font-semibold">Map columns</h3><div className="grid grid-cols-2 gap-3 mt-2">{req.map(f=> (<div key={f}><label className="text-xs text-slate-600">{f}</label><select className="w-full border rounded p-2 mt-1" onChange={e=>setMap(prev=>({...prev,[f]:e.target.value}))}><option value="">-- select --</option>{headers.map(h=><option key={h} value={h}>{h}</option>)}</select></div>))}</div><div className="mt-3"><button className="btn btn-primary" onClick={()=>onMap(map)}>Save mapping</button></div></div>);} 
